#include <bits/stdc++.h>

#define pb push_back
#define sz(v) ((int)(v).size())
#define all(v) (v).begin(),(v).end()
#define mp make_pair

using namespace std;

class CPetyaAndExam {
public:
    void solve(std::istream &in, std::ostream &out) {

        int t;
        in >> t;
        while (t--) {
            int n, t, a, b;
            in >> n >> t >> a >> b;

            vector<int> hard(n);
            vector<int> v(n);

            for (int i = 0; i < n; ++i)in >> hard[i];
            for (int i = 0; i < n; ++i)in >> v[i];



        }
    }
};